# Python
__pycache__/
*.py[cod]
*.so
*.egg-info/
dist/
build/
.eggs/

# Django
db.sqlite3
db.sqlite3-journal
*.log
local_settings.py

# 环境变量
.env
.env.local

# 项目本地数据 —— 不提交音乐文件、缓存
music_library/*
!music_library/.gitkeep
cache/
*.lrc.cache

# 静态文件收集目录（如果有）
staticfiles/
collected_static/

# 编辑器 / IDE
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# 系统文件
.Trashes
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# ===== 以下为上传 GitHub 前补充（避免泄露密钥 / 超大文件）=====
# 云厂商密钥等本地配置（只提交 tools/cloud_config.example.json 模板）
tools/cloud_config.json
tools/ngrok/
*.exe
*.dll

# 交付包副本（源码在仓库根目录，无需重复提交）
云律AI交付包/
*.zip
*.rar
*.7z

# 运行时生成的封面图 / 日志（运行后会自动生成，不必入库）
media/
server*.err
server*.out
*.err
*.out
